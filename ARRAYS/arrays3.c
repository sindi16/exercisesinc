#include <stdio.h>

//Loop through an array.
int main(void)
{
    int = i;    //loop counter

    int valueofitem[] = {30, 25, 15, 32, 24};
    for (i = 0; i < 5; i++)
    {
        printf("%d\n", valueofitem[i]);
    }
    return (0);
}
 // Now outputs 33 instead of 15.
#include <stdio.h>

//to get array size or length...
int main(void)
{                   // 0  1  2  3
    int mynumbers[] = {3, 5, 7, 9};
    int length = sizeof(mynumbers)/sizeof(mynumbers)[0];
    //size 16/4=4 lengthi
    int =i;
    for (i = 0; i < length, i++)
        printf("%d\n")
    return (0);
}


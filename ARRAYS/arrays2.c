#include <stdio.h>

//some examples in c programming for arrays.
//To print an element of array.
int main(void)
{
    int myage[] = {25 , 35 ,47, 10, 5}; //We have now created a variable that holds an array of four integers.
    printf("%d\n", myage[2]); //To access an array element, refer to its index number.
    return (0);
}